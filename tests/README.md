# Tests

This directory will contain tests for schemas, workflow examples, evidence pack structure, and future source code.

Initial test priorities:

1. Validate all JSON schemas are syntactically valid.
2. Validate sample product profiles against `product_profile.schema.json`.
3. Validate sample validation protocol drafts against `validation_protocol.schema.json`.
4. Validate technical context cards against `technical_context_card.schema.json`.
5. Validate analytical report gap assessment outputs.
